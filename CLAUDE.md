# CodexBar（fork）项目约定

本文件是 fork 维护者的开发纪律，补充而不覆盖仓库根 `AGENTS.md` 的既有规范（构建/测试/风格以 AGENTS.md 为准）。

## 功能开发纪律
本项目以设计文档 `docs/claude-native-multi-account-clash.md` 为单一事实源,以测试文档 `docs/TEST.md` 为验收执行脚本与台账。任何功能新增或改动,必须按此序:先更新设计文档,再改实现代码,再同步测试文档中受影响的用例,并按测试文档执行测试(范围按其回归策略)、在其执行记录中登记轮次。仅改实现代码而未同步两份文档、或未执行并登记测试的改动,视为未完成;设计文档已正确覆盖本次改动(如实现追齐设计的缺陷修复)时,设计文档可不动,在交付说明注明即可。更新设计文档时保持其既有章节结构、ID 约定与 Mermaid 图,状态图与迁移表、图与正文保持一致;更新测试文档时保持用例格式与 TC→REQ 追溯,执行记录只增不改。

（范围说明:上游同步/cherry-pick 的既有功能不受此纪律约束;fork 自研功能域——当前为 Claude 原生多账号 + Clash 联动——新增功能域时并入同一份设计文档与测试文档,ID 带域段。）

## 本机工具链约束（fork 特有）
本机为 macOS 15.3.2 + Xcode 16.2（Swift 6.0.3），上游要求 Swift 6.2。本 fork 已做降级适配：`Package.swift` 工具版本降到 6.0、`KeyboardShortcuts` 钉 2.3.0、`swift-log` 钉 1.8.0（日志 handler 已改回旧版 `log(level:...)` API）、依赖旁路的 `../SweetCookieKit` 本地克隆（其 Package.swift 同样降到 6.0，勿删该目录）。同步上游时若这些文件冲突，保留本 fork 的降级改动；若日后升级 Xcode ≥26，可整体回退这批适配（git log 搜 "Swift 6.0"）。

## 密钥纪律
运行时密钥只进 `.env`(已 gitignore),仓内只放 `.env.example` 登记「键名 + 用途 + 获取方式」;跨项目共享密钥集中存于 Bitwarden(item 名 = 键名),读取用 `bw get password <键名>` 管道直写、任何输出不回显值;主密码与解锁只由人执行,Agent 不询问主密码、不打印任何密钥值;发现仓内明文密钥立即停下提示轮换(git 历史同样已泄漏)。本项目特有:用户 Claude 账号凭据只存 macOS 钥匙串,聊天/代码/文档/日志零 token 零邮箱(设计文档 REQ-008)。
